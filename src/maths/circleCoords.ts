
/**
 * Etend les types de paper avec un cercle (centre + rayon)
 */
import Point = paper.Point;

export class CircleCoords {

    public constructor(public center : Point, public radius : number) {
    }

}